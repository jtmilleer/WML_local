Attempting to get WML off singlulariy

Running into a few issues, mainly a package called fsl is having troubles being downloaded. It seems like WML uses an older version. 

Extraction of fsl files from singulary file is possible, but they are big, I think around a few gb.

The other extracted files are pretty big aswell. I wondering if providing prebuilt image would be the way to go. Can just run without having to build locally. 



